# Java Shortest Path Algorithm

#### About
This application contains three algorithms. Given a 2 by 2 matrix with obstacles and a start and end location, the application calculates the length of the shortest path, the amount of obstacle clusters / islands and the size of the largest obstacle cluster / island.

The user has to configure the size of the matrix. The user can either specify the starting location, the ending location and the locations of the obstacles, or randomly generate those locations.
